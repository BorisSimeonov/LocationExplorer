@*@model AdminUserListingsViewModel*@

@{
    ViewData["Title"] = "Users List";
}

@*<table class="table table-bordered table-striped table-hover table-responsive text-center">
    <thead>
        <tr>
            <th class="text-center text-info">Username</th>
            <th class="text-center text-info">Email</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    @foreach (var user in Model.Users)
    {
        <tr>
            <td>@user.Username</td>
            <td>@user.Email</td>
            <td>
                <form asp-controller="Users" asp-action="AddToRole" method="post">
                    <input type="hidden" name="UserId" value="@user.Id"/>
                    <div class="form-group col-lg-8 col-md-8 col-sm-12 col-xs-12">
                        <select asp-items="Model.Roles" name="role" class="form-control"></select>
                    </div>
                    <div class="form-group col-lg-4 col-md-4 col-sm-12 col-xs-12">
                        <input type="submit" class="btn btn-primary" value="Add Selected Role"/>
                    </div>
                </form>
            </td>
        </tr>
    }
    </tbody>
</table>*@